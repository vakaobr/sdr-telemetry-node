/**
 * App shell — Phase 1 placeholder.
 * The live map/list views land in Phase 4 per 04_IMPLEMENTATION_PLAN.
 */
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem", background: "#0b0e14", color: "#e6e6e6", minHeight: "100vh" }}>
      <h1>sdr-telemetry-node</h1>
      <p>Dashboard scaffold ready — awaiting Phase 4 (live map &amp; list).</p>
    </main>
  );
}
